
接口设计：

* 提供句柄，通过句柄调用属性方法解决问题

fd = open()    fd为句柄

* 实例化对象，通过对象设置，启动服务

t  = Thread()
s = socketserver()

* 根据功能需求，如果无法替用户决定的内容，通过参数传递数据
*能够解决的问题，不要让用户去解决，需要用户解决的问题可以用重写的方法让用户重写


技术分析：

1. HTTP 协议
2. select 并发
3. tcp  套接字
